
name: Safe Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        shell: bash
        run: |
          echo "This is a safe test workflow."
          echo "If this runs, your YAML structure is fixed."
